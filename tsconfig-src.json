{
  "extends": "@aidc-toolkit/dev/tsconfig-template.json",
  "include": ["./src/**/*"],
  "compilerOptions": {
    // Emit.
    "outDir": "dist",

    // Language and environment.
    "experimentalDecorators": true
  }
}
